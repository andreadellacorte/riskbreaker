{
  "name": "@riskbreaker/plugin-vagrant-story",
  "private": true,
  "version": "0.0.0",
  "description": "Mock Vagrant Story plugin — reference implementation with JSON fixtures",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": ["dist", "src/fixtures"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@riskbreaker/plugin-sdk": "workspace:*",
    "@riskbreaker/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
