# Migration: 0.18.5 → 0.18.6

## What changed

v0.18.6 creates `.groove/memory/docs/` during `/groove-utilities-memory-install` so it matches `/groove-work-doc` (reference docs live there). Existing installs need the directory once.

## Outcome

`.groove/memory/docs/` exists (empty is fine).

## Steps

### 1. Create docs directory

```bash
mkdir -p .groove/memory/docs
```

If the directory already exists, skip silently.

Report: `✓ .groove/memory/docs/ ready`

## Constraints

- Idempotent — safe to re-run
- Do not modify `.groove/index.md` for this migration
