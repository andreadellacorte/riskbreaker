# Migration 0.10.0 → 0.10.1

Add `recent_memory_days: 5` to `.groove/index.md` and update `AGENTS.md` prime stub to reference `/groove-utilities-prime`.

## Outcome

`.groove/index.md` contains `recent_memory_days: 5` and `AGENTS.md` bootstrap references the correct skill name.

## Acceptance Criteria

- `.groove/index.md` frontmatter contains `recent_memory_days: 5`
- `AGENTS.md` `<!-- groove:prime:start -->` stub references `/groove-utilities-prime`
- `.groove/index.md` `groove-version:` is `0.10.1` (set by groove update after this migration)

## Steps

1. Read `.groove/index.md` frontmatter
2. If `recent_memory_days:` key is absent: add `recent_memory_days: 5` to the frontmatter (after the `memory:` line if present, otherwise at end of frontmatter)
3. If `recent_memory_days:` key is already present: skip silently
4. Read `AGENTS.md` at git root
5. Locate the `<!-- groove:prime:start -->` / `<!-- groove:prime:end -->` section
6. If the section contains `/groove prime` or `/groove:prime` (old format): replace with `/groove-utilities-prime`
7. If already updated or absent: skip silently

## Constraints

- Idempotent — safe to re-run; if keys already present or already updated, do nothing
- Do not modify any other content in `.groove/index.md` or `AGENTS.md`
- Do not modify `.groove/index.md` `groove-version:` — that is handled by `groove update` after this migration completes
