# Migration: 0.16.1 → 0.17.0

## What changed

v0.17.0 removes the `<!-- groove:prime -->` and `<!-- groove:task -->` AGENTS.md stubs. Session bootstrap is now handled entirely by native platform hooks (Claude Code SessionStart, Cursor hooks). AGENTS.md no longer needs these sections.

## Outcome

`AGENTS.md` has the `groove:prime` and `groove:task` fenced sections removed. All other content is preserved.

## Steps

### 1. Remove groove:prime section from AGENTS.md

If `AGENTS.md` exists and contains `<!-- groove:prime:start -->`:

- Remove everything from `<!-- groove:prime:start -->` through `<!-- groove:prime:end -->` (inclusive), plus any trailing blank line
- Preserve all other content

### 2. Remove groove:task section from AGENTS.md

If `AGENTS.md` contains `<!-- groove:task:start -->`:

- Remove everything from `<!-- groove:task:start -->` through `<!-- groove:task:end -->` (inclusive), plus any trailing blank line
- Preserve all other content

## Constraints

- Idempotent — if sections are already absent, do nothing
- Preserve all other AGENTS.md content exactly
- Do not delete AGENTS.md even if it becomes empty after removal
