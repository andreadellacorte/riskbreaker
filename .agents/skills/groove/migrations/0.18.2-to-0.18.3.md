# Migration: 0.18.2 → 0.18.3

## What changed

v0.18.3 renames the `tasks.backend` config key to `tasks.storage` for clarity.

## Outcome

`.groove/index.md` has `tasks.storage:` instead of `tasks.backend:`, with the same value preserved.

## Steps

### 1. Rename `tasks.backend` to `tasks.storage`

In `.groove/index.md`, find the line `  backend: <value>` under the `tasks:` key and rename it to `  storage: <value>`.

If `tasks.backend` is not present (e.g. already renamed), skip silently.

Report: `✓ tasks.backend → tasks.storage`

## Constraints

- Idempotent — if `tasks.storage` already exists and `tasks.backend` does not, report done and exit
- Preserve the existing value exactly
