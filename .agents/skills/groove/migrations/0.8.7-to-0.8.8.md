# Migration: 0.8.7 → 0.8.8

## Outcome

The `<!-- groove:task:start -->` section in `AGENTS.md` uses imperative wording — "Run `beans prime` to load..." instead of "if you need...".

## Acceptance Criteria

- `<!-- groove:task:start -->` section reads:
  ```
  Task backend: beans — use `/groove:task` commands for all task management.
  Run `beans prime` to load the full beans CLI reference.
  ```
- All other `AGENTS.md` content is preserved

## Constraints

- This migration is idempotent — safe to re-run if interrupted
- If `AGENTS.md` does not exist, skip

### Step 1 — Rewrite `groove:task` section

Replace the entire `<!-- groove:task:start -->` / `<!-- groove:task:end -->` block with:

```
<!-- groove:task:start -->
Task backend: beans — use `/groove:task` commands for all task management.
Run `beans prime` to load the full beans CLI reference.
<!-- groove:task:end -->
```

If the section is absent, skip.
