apiVersion: v1
kind: Service
metadata:
  name: riskbreaker-web
  labels:
    app.kubernetes.io/name: riskbreaker-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: riskbreaker-web
  ports:
    - name: http
      port: 80
      targetPort: http
