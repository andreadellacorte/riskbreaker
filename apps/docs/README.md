# @riskbreaker/docs

Static Vite placeholder for documentation. **`pnpm dev:docs`** from the repo root serves http://localhost:5174 .

Authoritative docs remain in `project-spec.md` and `.groove/memory/` until Harness 08 expands this app.
