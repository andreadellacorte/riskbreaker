# @riskbreaker/save-service

Phase 1 helpers around **`ISaveCodec`**:

- **`SaveSlotBrowser`** — decode raw slot bytes through the plugin codec (or a stub).
- **`MockBackupStore`** — placeholder for backup/rollback flows.
