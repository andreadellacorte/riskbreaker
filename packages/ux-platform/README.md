# @riskbreaker/ux-platform

Thin UI-adjacent types for Phase 1:

- **`ScreenRegistry`** — plugin screens (ids + metadata).
- **`InputMappingModel`** — placeholder for keyboard/gamepad bindings.

Apps consume view models from **`@riskbreaker/domain-engine`**, not raw runtime memory.
