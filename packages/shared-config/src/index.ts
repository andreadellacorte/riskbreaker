/**
 * Marker export so the workspace has TypeScript to typecheck in Harness 01.
 * Shared ESLint/Prettier presets live alongside this package; see `exports` in package.json.
 */
export const SHARED_CONFIG_VERSION = 1;
