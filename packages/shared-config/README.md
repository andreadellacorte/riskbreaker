# @riskbreaker/shared-config

Prettier preset (`prettier.config.mjs`, re-exported from the repo root). ESLint is configured at the repository root (`eslint.config.mjs`) so a single flat config applies to all packages.
