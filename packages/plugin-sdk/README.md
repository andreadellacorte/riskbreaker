# @riskbreaker/plugin-sdk

Plugin-facing **contracts** (`IGamePlugin`, packs, codecs) and **registration** helpers (`PluginRegistration`, `resolvePluginForManifest`). Depends only on `@riskbreaker/shared-types`.
