# @riskbreaker/devtools

Lightweight observability for Phase 1 mocks:

- **`EventTimeline`** — ordered `{ kind, payload }` events.
- **`createLogger`** — structured log records (default no-op sink; pass your own).
