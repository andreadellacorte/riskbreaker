{
  "name": "riskbreaker",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm --filter @riskbreaker/web dev",
    "dev:docs": "pnpm --filter @riskbreaker/docs dev",
    "build": "pnpm -r run build",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:playstation": "prettier --write apps/web/public/playstation/PlayStation.js --ignore-path /dev/null",
    "build:playstation": "node scripts/build-playstation.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p apps/web/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.58.2",
    "@riskbreaker/app-shell": "workspace:*",
    "@riskbreaker/plugin-vagrant-story": "workspace:*",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.12",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.1",
    "vitest": "^4.1.0"
  }
}
