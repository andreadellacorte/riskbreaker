scratch
.DS_Store
bins/

# Local environment — contains machine-specific paths (see .env.example)
.env
.env.local

node_modules
dist
coverage
playwright-report/
test-results/
blob-report/
*.tsbuildinfo
.turbo
.direnv

# Nix: `nix build` / flake output symlinks (`result`, `result-doc`, …)
/result
/result-*
result
result-*

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!*.tfvars.example
# Local Netlify folder
.netlify

# Generated PCSX kxkx artifacts (build outputs; do not commit)
packages/pcsx-wasm-core/pcsx_worker.js
packages/pcsx-wasm-core/pcsx_worker.wasm
packages/pcsx-wasm-core/pcsx_ww.js
packages/pcsx-wasm-core/pcsx_ww.wasm

# PCSX-wasm core runtime under public/ is tracked so Netlify/CI can build without Nix.
# Regenerate after changing packages/pcsx-wasm-core: pnpm -w run ensure:pcsx-wasm (inside nix OR with emcc on PATH).

# Generated PCSX kxkx artifacts materialized into dist/ (do not commit)
packages/pcsx-wasm-core/dist/**

# Riskbreaker overlay boot bundle (build artifact)
apps/web/public/pcsx-wasm/js/riskbreaker-pcsx-wasm-boot.js

